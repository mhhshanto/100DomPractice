function chng(){
  root.style.backgroundColor = rgb()
  }
  
  function rgb(){
    const red = Math.floor(Math.random()*999999)
    if(red.length = 5){

      continue
   
  }
  
  return `#${red}}`
  }
  console.log(rgb());